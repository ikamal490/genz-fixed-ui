<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Payment Successful 💳</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f4f8f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      flex-direction: column;
      color: #333;
    }

    .container {
      background: #fff;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      text-align: center;
      animation: fadeIn 0.8s ease;
    }

    h1 {
      color: #28a745;
      margin-bottom: 20px;
    }

    .details {
      text-align: left;
      margin-top: 20px;
    }

    .details p {
      margin: 5px 0;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      background: #28a745;
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      transition: background 0.3s ease;
    }

    a:hover {
      background: #218838;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>✅ Payment Successful!</h1>
    <p>Thank you for shopping with <strong>GenZ Clothing</strong>.</p>

    <div class="details">
      <p><strong>Order ID:</strong> <%= query.razorpay_order_id %></p>
      <p><strong>Payment ID:</strong> <%= query.razorpay_payment_id %></p>
      <p><strong>Amount:</strong> ₹<%= query.amount %></p>
    </div>

    <a href="/">Continue Shopping</a>
  </div>
</body>
</html>
